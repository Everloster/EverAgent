# Obsidian
.obsidian/workspace.json
.obsidian/core-plugins.json
.obsidian/appearance.json
.obsidian/app.json

# Claude
.claude/settings.local.json

# OS generated
.DS_Store
*/.DS_Store
**/.DS_Store
Thumbs.db

# Environment & secrets
.env
.env.*

# Archives
*.zip

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
venv/
.venv/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Agent runtime files (not committed)
*/.agent-lock
*/.agent-state

# Local cache for research/scripts (e.g. mmx search snippets)
.cache/

# Firecrawl scrape cache (transient fetch output, not a source of truth)
.firecrawl/

# 灰色来源书籍：仅本地学习使用，不入库（版权归出版方，公共仓库不可传播）
philosophy-learning/books/儒法国家_中国历史新论_赵鼎新_2022.epub

# 基础设施与设备家底（含 AWS 账号/IP/SSH/Tailscale 等敏感面）已拆至独立私有仓 EverAgent-infra。
# 正常两仓并排放（私有仓在本仓目录之外）即无风险；此处兜底防「把私有仓档案复制/软链进公开仓根目录」的手滑。
/infra/
# 私有仓根目录的 VPS 管理档案（用泛化模式兜底，不在公开仓写出真实文件名）
/*VPS*.md
/*vps*.md

# 外部工具自动生成的杂项（botlearn cron 路径配置错误会每天重建 .botlearn/；Qoder IDE 项目目录；来历不明的 0 字节文件）
/.botlearn/
/.qoder/
/-l
