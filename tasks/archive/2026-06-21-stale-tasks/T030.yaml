apiVersion: everagent.io/v1
kind: Task
metadata:
  id: T030
  project: ai-learning
  status: archived
  archived_at: 2026-06-21
  archive_reason: "任务已 done（claimed_by NeuronAgent, done_at 2026-04-23），报告 ai-learning/reports/knowledge_reports/Agent_心跳检测与实时Dashboard设计.md（425 行）已存在；tasks/ 中此 YAML 滞后于 .project-task-state，造成 2026-06-21 误派发并触发 help_needed。归档后从下次调度器候选中排除。"
spec:
  type: knowledge_report
  target: "Agent 心跳监控与实时可观测性架构设计"
  value: "基于 Phase 1 事件日志层，设计 Agent 心跳检测机制、实时 Dashboard 数据流、以及任务执行延迟告警系统"
  priority: P2
  dependencies:
    - task: T020
      condition: done
  resources:
    max_tokens: 30000
    expected_duration: 2h
  qualityGates:
    - check: frontmatter_complete
      required: true
    - check: wiki_linked
      required: false
  retryPolicy:
    maxRetries: 1
    backoff: fixed
