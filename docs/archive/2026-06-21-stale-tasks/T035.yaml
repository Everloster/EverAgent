apiVersion: everagent.io/v1
kind: Task
metadata:
  id: T035
  project: cs-learning
  status: archived
  archived_at: 2026-06-21
  archive_reason: "任务已 done（claimed_by ByteAgent, done_at 2026-04-22），报告 cs-learning/reports/knowledge_reports/编程语言范式演化_从Lisp到Go的并发模型变迁.md 已存在；tasks/ 中此 YAML 滞后于 .project-task-state，造成 2026-06-21 误派发并触发 help_needed。归档后从下次调度器候选中排除。"
spec:
  type: knowledge_report
  target: "编程语言范式演化：从Lisp到Go的并发模型变迁"
  value: "整合 McCarthy(1960)/Dijkstra(1968)/Hoare(1978)/Lattner(2004) 等关键论文; 函数式/结构化/CSP/LLVM四阶段演化图谱"
  priority: P2
  resources:
    max_tokens: 50000
    expected_duration: 3h
  qualityGates:
    - check: frontmatter_complete
      required: true
    - check: mermaid_diagrams
      required: true
